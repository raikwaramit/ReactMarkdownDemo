import { SwipeableDrawer } from "@mui/material";
import * as React from "react";

export interface IDrawableComponentProps {}

export default function DrawableComponent(props: IDrawableComponentProps) {
  return (
    <div>
      
    </div>
  );
}
